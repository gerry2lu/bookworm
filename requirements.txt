# BookWorm Reading Facilitator Requirements
# For Raspberry Pi 5 with local processing
# Clean minimal dependencies - no database requirements

# Core AI and audio processing
openai-whisper>=20231117
sounddevice>=0.4.6
numpy>=1.21.0
requests>=2.28.0

# Voice activity detection
webrtcvad>=2.0.10

# Audio processing (choose one - sounddevice is preferred)
# pyaudio>=0.2.11  # Alternative to sounddevice, uncomment if needed

# Hardware interfaces for Raspberry Pi
RPi.GPIO>=0.7.1

# Optional: Enhanced audio processing (only if needed)
# scipy>=1.10.0
# librosa>=0.9.0

# Optional: PiicoDev RFID (install manually if available)
# piicodev

# System utilities (optional)
# psutil>=5.9.0

# IMPORTANT NOTES:
# 1. Install Piper TTS separately from GitHub releases
# 2. Install Ollama separately: curl -fsSL https://ollama.ai/install.sh | sh
# 3. Pull model: ollama pull gemma2:2b
# 4. For PiicoDev: pip install piicodev (if available)